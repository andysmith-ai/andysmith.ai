{
  "name": "andysmith-ai-blog",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf _site && eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.114.0"
  }
}
